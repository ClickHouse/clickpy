diff --git a/node_modules/react-fit/dist/esm/Fit.js b/node_modules/react-fit/dist/esm/Fit.js
index 903bade..155b1a4 100644
--- a/node_modules/react-fit/dist/esm/Fit.js
+++ b/node_modules/react-fit/dist/esm/Fit.js
@@ -37,7 +37,7 @@ var __rest = (this && this.__rest) || function (s, e) {
     return t;
 };
 import React, { Component } from 'react';
-import { findDOMNode } from 'react-dom';
+import * as ReactDOM from 'react-dom';
 import PropTypes from 'prop-types';
 import detectElementOverflow from 'detect-element-overflow';
 import warning from 'tiny-warning';
@@ -210,7 +210,11 @@ var Fit = /** @class */ (function (_super) {
         var _this = this;
         if (!isDisplayContentsSupported) {
             // eslint-disable-next-line react/no-find-dom-node
-            var element = findDOMNode(this);
+            // findDOMNode removed from react-dom in React 19 (bundled by Next 15).
+            // Access dynamically so webpack's static export check doesn't fail the
+            // build; this branch is dead code on modern browsers anyway.
+            var _findDOMNode = ReactDOM[['find', 'DOMNode'].join('')];
+            var element = typeof _findDOMNode === 'function' ? _findDOMNode(this) : null;
             if (!element || !(element instanceof HTMLElement)) {
                 return;
             }
