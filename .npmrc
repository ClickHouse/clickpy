# Some dependencies still declare React 18 peer ranges even though they run
# fine on the React 19 that Next 15 bundles. legacy-peer-deps lets the install
# resolve like npm v6 instead of erroring on those stale peer ranges.
legacy-peer-deps=true
